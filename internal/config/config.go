package config

// Dir returns the orbit configuration directory path.
func Dir() (string, error) {
	return "", nil
}

// DatabasePath returns the full path to the orbit database file.
func DatabasePath() (string, error) {
	return "", nil
}
